package:
  name: ros-galactic-nav2-theta-star-planner
  version: 1.0.12
source:
  git_url: https://github.com/SteveMacenski/navigation2-release.git
  git_rev: release/galactic/nav2_theta_star_planner/1.0.12-1
  folder: ros-galactic-nav2-theta-star-planner/src/work

build:
  script:
    sel(win): bld_ament_cmake.bat
    sel(unix): build_ament_cmake.sh
  number: 1
about:
  home: https://www.ros.org/
  license: BSD-3-Clause
  summary: |
    Robot Operating System

extra:
  recipe-maintainers:
    - ros-forge

requirements:
  build:
    - "{{ compiler('cxx') }}"
    - "{{ compiler('c') }}"
    - ninja
    - sel(unix): make
    - sel(osx): tapi
    - cmake 3.23
    - sel(build_platform != target_platform): python
    - sel(build_platform != target_platform): cross-python_{{ target_platform }}
    - sel(build_platform != target_platform): cython
  host:
    - python
    - ros-galactic-ament-cmake
    - ros-galactic-ament-cmake-gtest
    - ros-galactic-ament-lint-auto
    - ros-galactic-ament-lint-common
    - ros-galactic-builtin-interfaces
    - ros-galactic-nav2-common
    - ros-galactic-nav2-core
    - ros-galactic-nav2-costmap-2d
    - ros-galactic-nav2-msgs
    - ros-galactic-nav2-util
    - ros-galactic-pluginlib
    - ros-galactic-rclcpp
    - ros-galactic-rclcpp-action
    - ros-galactic-rclcpp-lifecycle
    - ros-galactic-ros-environment
    - ros-galactic-ros-workspace
    - ros-galactic-tf2-ros
    - ros2-distro-mutex 0.2 galactic
  run:
    - python
    - ros-galactic-builtin-interfaces
    - ros-galactic-nav2-common
    - ros-galactic-nav2-core
    - ros-galactic-nav2-costmap-2d
    - ros-galactic-nav2-msgs
    - ros-galactic-nav2-util
    - ros-galactic-pluginlib
    - ros-galactic-rclcpp
    - ros-galactic-rclcpp-action
    - ros-galactic-rclcpp-lifecycle
    - ros-galactic-ros-workspace
    - ros-galactic-tf2-ros
    - ros2-distro-mutex 0.2 galactic
    - sel(osx and x86_64): __osx >={{ MACOSX_DEPLOYMENT_TARGET|default('10.14') }}
